{
  "name": "no-deploy-fridays",
  "version": "1.0.0",
  "description": "A GitHub Action that blocks deployments on Fridays, configured holidays, and the last working day of the week when holidays shift it earlier.",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "license": "MIT",
  "author": "rorycaraher",
  "keywords": [
    "github-actions",
    "deployment",
    "ci",
    "holidays"
  ],
  "dependencies": {
    "@actions/core": "^1.10.1",
    "js-yaml": "^4.1.0",
    "luxon": "^3.5.0",
    "node-ical": "^0.18.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0"
  }
}
